"use client";

// components/Header.tsx
import { DatePicker, Card } from 'antd';

const Header = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'space-between', padding: '20px' }}>
      <div style={{ fontSize: '24px', color: '#F57C00' }}>Sales Analytics</div>
    </div>
  );
};

export default Header;
